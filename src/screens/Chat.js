import React from 'react';
import { View, Text } from 'react-native';

function Chat() {
  return (
    <View style={{
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
    }}>
      <Text>Chat View</Text>
    </View>
  );
}

export default Chat;